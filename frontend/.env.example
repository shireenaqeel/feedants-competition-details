# Base URL of the backend API. Optional in development:
# leave it unset and the app uses http://<machine running Expo>:4000 automatically
# (works for web, simulators and Expo Go on a phone on the same Wi-Fi).
# Set it only to point at a different backend, e.g. a deployed one.
# EXPO_PUBLIC_API_URL=https://api.example.com
